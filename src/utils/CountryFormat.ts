const UkFormat = () => {};
const UsFormat = () => {};

const RuFomat = () => {
  const areaCode = "7"; // Russia country code
  const validFirstDigits = ["9"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000000) + 100000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Germany = () => {
  const areaCode = "49"; // Russia country code
  const validFirstDigits = ["15", "16"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000000) + 100000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const UnitedKingdom = () => {
  const areaCode = "44"; // Russia country code
  const validFirstDigits = ["07"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000000) + 100000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const France = () => {
  const areaCode = "33"; // Russia country code
  const validFirstDigits = ["6", "7"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Italy = () => {
  const areaCode = "39"; // Russia country code
  const validFirstDigits = ["3"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Spain = () => {
  const areaCode = "34"; // Russia country code
  const validFirstDigits = ["6", "7"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Poland = () => {
  const areaCode = "48"; // Russia country code
  const validFirstDigits = ["5"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Ukraine = () => {
  const areaCode = "380"; // Russia country code
  const validFirstDigits = ["5", "6"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Romania = () => {
  const areaCode = "40"; // Russia country code
  const validFirstDigits = ["7"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000000) + 100000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Netherlands = () => {
  const areaCode = "31"; // Russia country code
  const validFirstDigits = ["6"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Belguim = () => {
  const areaCode = "32"; // Russia country code
  const validFirstDigits = ["4"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Sweden = () => {
  const areaCode = "46"; // Russia country code
  const validFirstDigits = ["7"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};
const Czechia = () => {
  const areaCode = "420"; // Russia country code
  const validFirstDigits = ["6", "7"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};
const Greece = () => {
  const areaCode = "30"; // Russia country code
  const validFirstDigits = ["6"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Portugal = () => {
  const areaCode = "351"; // Russia country code
  const validFirstDigits = ["9"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Hungary = () => {
  const areaCode = "36"; // Russia country code
  const validFirstDigits = ["20", "30"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Austria = () => {
  const areaCode = "36"; // Russia country code
  const validFirstDigits = ["20", "30"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Switzerland = () => {
  const areaCode = "41"; // Russia country code
  const validFirstDigits = ["7"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Serbia = () => {
  const areaCode = "381"; // Russia country code
  const validFirstDigits = ["6"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Bulgaria = () => {
  const areaCode = "359"; // Russia country code
  const validFirstDigits = ["87", "88", "89"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Denmark = () => {
  const areaCode = "45"; // Russia country code
  const validFirstDigits = ["2"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Slovakia = () => {
  const areaCode = "421"; // Russia country code
  const validFirstDigits = ["9"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Finland = () => {
  const areaCode = "358"; // Russia country code
  const validFirstDigits = ["4", "5"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Norway = () => {
  const areaCode = "47"; // Russia country code
  const validFirstDigits = ["4", "9", "59"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Ireland = () => {
  const areaCode = "353"; // Russia country code
  const validFirstDigits = ["8"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Croatia = () => {
  const areaCode = "385"; // Russia country code
  const validFirstDigits = ["9"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Moldova = () => {
  const areaCode = "373"; // Russia country code
  const validFirstDigits = ["6"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Bosnia = () => {
  const areaCode = "387"; // Russia country code
  const validFirstDigits = ["6"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Albania = () => {
  const areaCode = "355"; // Russia country code
  const validFirstDigits = ["6"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Lithuania = () => {
  const areaCode = "370"; // Russia country code
  const validFirstDigits = ["6"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Slovenia = () => {
  const areaCode = "386"; // Russia country code
  const validFirstDigits = ["3"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Macedonia = () => {
  const areaCode = "389"; // Russia country code
  const validFirstDigits = ["7"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Latvia = () => {
  const areaCode = "371"; // Russia country code
  const validFirstDigits = ["2"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Estonia = () => {
  const areaCode = "372"; // Russia country code
  const validFirstDigits = ["5"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Luxembourg = () => {
  const areaCode = "352"; // Russia country code
  const validFirstDigits = ["6"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Montenegro = () => {
  const areaCode = "382"; // Russia country code
  const validFirstDigits = ["6", "7"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Malta = () => {
  const areaCode = "356"; // Russia country code
  const validFirstDigits = ["7"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};
const Iceland = () => {
  const areaCode = "354"; // Russia country code
  const validFirstDigits = ["6"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Andorra = () => {
  const areaCode = "376"; // Russia country code
  const validFirstDigits = ["3", "4", "6"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000) + 10000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Liechtenstein = () => {
  const areaCode = "423"; // Russia country code
  const validFirstDigits = ["6"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Monaco = () => {
  const areaCode = "377"; // Russia country code
  const validFirstDigits = ["6"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Marino = () => {
  const areaCode = "378"; // Russia country code
  const validFirstDigits = ["3", "6"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Holy = () => {
  const areaCode = "379"; // Russia country code
  const validFirstDigits = ["3", "6"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Isle = () => {
  const areaCode = "44"; // Russia country code
  const validFirstDigits = ["07624"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Faroe = () => {
  const areaCode = "298"; // Russia country code
  const validFirstDigits = ["2"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000) + 10000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Gibraltar = () => {
  const areaCode = "350"; // Russia country code
  const validFirstDigits = ["5", "6"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};
const InFormat = () => {
  const areaCode = "91"; // India's country code
  const validFirstDigits = ["7", "8", "9", "6"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000000) + 100000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};
const HkFormat = () => {
  const areaCode = "852"; // Hong Kong's country code
  const validFirstDigits = ["5", "6", "9"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

/////////// Asia Countries //////////////
/////////////////////////////////////////

const Afghanistan = () => {
  const areaCode = "93"; // Hong Kong's country code
  const validFirstDigits = ["7"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Armenia = () => {
  const areaCode = "374"; // Hong Kong's country code
  const validFirstDigits = ["55", "77"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Azerbaijan = () => {
  const areaCode = "994"; // Hong Kong's country code
  const validFirstDigits = ["50", "51"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Bahrain = () => {
  const areaCode = "973"; // Hong Kong's country code
  const validFirstDigits = ["3"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Bangladesh = () => {
  const areaCode = "880"; // Hong Kong's country code
  const validFirstDigits = ["1"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Bhutan = () => {
  const areaCode = "975"; // Hong Kong's country code
  const validFirstDigits = ["17", "18"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Brunei = () => {
  const areaCode = "673"; // Hong Kong's country code
  const validFirstDigits = ["71", "73"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000) + 10000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Cambodia = () => {
  const areaCode = "855"; // Hong Kong's country code
  const validFirstDigits = ["1"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const China = () => {
  const areaCode = "86"; // Hong Kong's country code
  const validFirstDigits = ["13", "18"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Georgia = () => {
  const areaCode = "995"; // Hong Kong's country code
  const validFirstDigits = ["5"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Indonesia = () => {
  const areaCode = "62"; // Hong Kong's country code
  const validFirstDigits = ["8"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Iran = () => {
  const areaCode = "98"; // Hong Kong's country code
  const validFirstDigits = ["9"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000000) + 1000000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Iraq = () => {
  const areaCode = "964"; // Hong Kong's country code
  const validFirstDigits = ["7"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000000) + 100000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Israel = () => {
  const areaCode = "972"; // Hong Kong's country code
  const validFirstDigits = ["5"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Japan = () => {
  const areaCode = "81"; // Hong Kong's country code
  const validFirstDigits = ["70", "80"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Jordan = () => {
  const areaCode = "962"; // Hong Kong's country code
  const validFirstDigits = ["7"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Kazakhstan = () => {
  const areaCode = "7"; // Hong Kong's country code
  const validFirstDigits = ["7"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000000) + 100000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Kuwait = () => {
  const areaCode = "965"; // Hong Kong's country code
  const validFirstDigits = ["5", "6"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Kyrgyzstan = () => {
  const areaCode = "996"; // Hong Kong's country code
  const validFirstDigits = ["5"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Laos = () => {
  const areaCode = "856"; // Hong Kong's country code
  const validFirstDigits = ["20", "30"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Lebanon = () => {
  const areaCode = "961"; // Hong Kong's country code
  const validFirstDigits = ["70", "71"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000) + 10000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Macau = () => {
  const areaCode = "853"; // Hong Kong's country code
  const validFirstDigits = ["6"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};
const Malaysia = () => {
  const areaCode = "60"; // Hong Kong's country code
  const validFirstDigits = ["1"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Maldives = () => {
  const areaCode = "960"; // Hong Kong's country code
  const validFirstDigits = ["7"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Mongolia = () => {
  const areaCode = "976"; // Hong Kong's country code
  const validFirstDigits = ["88", "99"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Myanmar = () => {
  const areaCode = "95"; // Hong Kong's country code
  const validFirstDigits = ["9"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Nepal = () => {
  const areaCode = "977"; // Hong Kong's country code
  const validFirstDigits = ["98", "88"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const NorthKorea = () => {
  const areaCode = "850"; // Hong Kong's country code
  const validFirstDigits = ["19", "88"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Oman = () => {
  const areaCode = "968"; // Hong Kong's country code
  const validFirstDigits = ["9"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Pakistan = () => {
  const areaCode = "92"; // Hong Kong's country code
  const validFirstDigits = ["3"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000000) + 100000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Palestine = () => {
  const areaCode = "970"; // Hong Kong's country code
  const validFirstDigits = ["5"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Philippines = () => {
  const areaCode = "63"; // Hong Kong's country code
  const validFirstDigits = ["9"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Qatar = () => {
  const areaCode = "974"; // Hong Kong's country code
  const validFirstDigits = ["3"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Saudi = () => {
  const areaCode = "966"; // Hong Kong's country code
  const validFirstDigits = ["5"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Singapore = () => {
  const areaCode = "65"; // Hong Kong's country code
  const validFirstDigits = ["8", "9"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};
// const SouthKorea = () => {
//   const areaCode = "82"; // Hong Kong's country code
//   const validFirstDigits = ["8", "9"];
//   const firstDigit =
//     validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
//   const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
//   return areaCode + firstDigit + number;
// };

const SriLanka = () => {
  const areaCode = "94"; // Hong Kong's country code
  const validFirstDigits = ["7"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Syria = () => {
  const areaCode = "963"; // Hong Kong's country code
  const validFirstDigits = ["9"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Taiwan = () => {
  const areaCode = "886"; // Hong Kong's country code
  const validFirstDigits = ["9"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

// const Tajikistan = () => {
//   const areaCode = "992"; // Hong Kong's country code
//   const validFirstDigits = ["9"];
//   const firstDigit =
//     validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
//   const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
//   return areaCode + firstDigit + number;
// };

const Thailand = () => {
  const areaCode = "66"; // Hong Kong's country code
  const validFirstDigits = ["6", "8", "9"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Timor = () => {
  const areaCode = "670"; // Hong Kong's country code
  const validFirstDigits = ["7"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Turkey = () => {
  const areaCode = "90"; // Hong Kong's country code
  const validFirstDigits = ["5"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000000) + 100000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const UnitedArabEmirates = () => {
  const areaCode = "971"; // Hong Kong's country code
  const validFirstDigits = ["5"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Vietnam = () => {
  const areaCode = "84"; // Hong Kong's country code
  const validFirstDigits = ["9"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 90000000) + 10000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Yemen = () => {
  const areaCode = "967"; // Hong Kong's country code
  const validFirstDigits = ["7"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 9000000) + 1000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

//////////// USA COUNTRIES ////////////////
///////////////////////////////////////////

const UnitedStates = () => {
  const areaCode = "1"; // Hong Kong's country code
  const validFirstDigits = ["2", "3"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000000) + 1000000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Canada = () => {
  const areaCode = "1"; // Hong Kong's country code
  const validFirstDigits = ["4", "6"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000000) + 1000000000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Bahamas = () => {
  const areaCode = "1"; // Hong Kong's country code
  const validFirstDigits = ["4", "5"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const SaintNevis = () => {
  const areaCode = "1"; // Hong Kong's country code
  const validFirstDigits = ["7"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Tobago = () => {
  const areaCode = "1"; // Hong Kong's country code
  const validFirstDigits = ["3", "4"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};
const Barbados = () => {
  const areaCode = "1"; // Hong Kong's country code
  const validFirstDigits = ["2", "3"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Antigua = () => {
  const areaCode = "1"; // Hong Kong's country code
  const validFirstDigits = ["7"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const Panama = () => {
  const areaCode = "507"; // Hong Kong's country code
  const validFirstDigits = ["6"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const CostaRica = () => {
  const areaCode = "506"; // Hong Kong's country code
  const validFirstDigits = ["6", "7"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};

const SaintLucia = () => {
  const areaCode = "1"; // Hong Kong's country code
  const validFirstDigits = ["5"];
  const firstDigit =
    validFirstDigits[Math.floor(Math.random() * validFirstDigits.length)];
  const number = Math.floor(Math.random() * 900000) + 100000; // Random 7-digit number
  return areaCode + firstDigit + number;
};
const CountrFormat = {
  HK: HkFormat,
  IN: InFormat,
  RU: RuFomat,
  DE: Germany,
  GB: UnitedKingdom,
  IT: Italy,
  FR: France,
  ES: Spain,
  PL: Poland,
  UA: Ukraine,
  RO: Romania,
  NL: Netherlands,
  BE: Belguim,
  SE: Sweden,
  CZ: Czechia,
  GR: Greece,
  PT: Portugal,
  HU: Hungary,
  AT: Austria,
  CH: Switzerland,
  RS: Serbia,
  BG: Bulgaria,
  DK: Denmark,
  SK: Slovakia,
  FIL: Finland,
  NO: Norway,
  IE: Ireland,
  HR: Croatia,
  MD: Moldova,
  BA: Bosnia,
  AL: Albania,
  LT: Lithuania,
  SI: Slovenia,
  MK: Macedonia,
  LV: Latvia,
  EE: Estonia,
  LU: Luxembourg,
  ME: Montenegro,
  MT: Malta,
  IS: Iceland,
  AD: Andorra,
  LI: Liechtenstein,
  MC: Monaco,
  SM: Marino,
  VA: Holy,
  IM: Isle,
  FO: Faroe,
  GI: Gibraltar,
  AF: Afghanistan,
  AM: Armenia,
  AZ: Azerbaijan,
  BH: Bahrain,
  BD: Bangladesh,
  BT: Bhutan,
  BN: Brunei,
  KH: Cambodia,
  CN: China,
  GE: Georgia,
  ID: Indonesia,
  IR: Iran,
  IQ: Iraq,
  IL: Israel,
  JP: Japan,
  JO: Jordan,
  KZ: Kazakhstan,
  KW: Kuwait,
  KG: Kyrgyzstan,
  LA: Laos,
  LB: Lebanon,
  MO: Macau,
  MY: Malaysia,
  MV: Maldives,
  MN: Mongolia,
  MM: Myanmar,
  NP: Nepal,
  KP: NorthKorea,
  OM: Oman,
  PK: Pakistan,
  PS: Palestine,
  PH: Philippines,
  QA: Qatar,
  SA: Saudi,
  SG: Singapore,
  LK: SriLanka,
  SY: Syria,
  TW: Taiwan,
  TH: Thailand,
  TL: Timor,
  TR: Turkey,
  AE: UnitedArabEmirates,
  VN: Vietnam,
  YE: Yemen,
  US: UnitedStates,
  CA: Canada,
  BS: Bahamas,
  KN: SaintNevis,
  TT: Tobago,
  BB: Barbados,
  AG: Antigua,
  PA: Panama,
  CR: CostaRica,
  LC: SaintLucia,
};
export default CountrFormat;
